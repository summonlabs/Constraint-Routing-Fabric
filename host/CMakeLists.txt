add_executable(crf_host crf_host.cpp)
target_link_libraries(crf_host PRIVATE ConstraintRoutingFabric crf_warnings)
target_include_directories(crf_host PRIVATE ${CMAKE_SOURCE_DIR}/tests)
set_target_properties(crf_host PROPERTIES CXX_STANDARD 20 CXX_STANDARD_REQUIRED ON CXX_EXTENSIONS OFF)
if(CRF_ENABLE_ASAN)
  crf_enable_asan(crf_host)
endif()
if(CRF_ENABLE_ANALYZE)
  target_compile_options(crf_host PRIVATE /analyze /analyze:external-)
endif()
