add_executable(crf_bench crf_bench.cpp)
target_link_libraries(crf_bench PRIVATE ConstraintRoutingFabric crf_warnings)
set_target_properties(crf_bench PROPERTIES CXX_STANDARD 20 CXX_STANDARD_REQUIRED ON CXX_EXTENSIONS OFF)
if(CRF_ENABLE_ASAN)
  crf_enable_asan(crf_bench)
endif()
